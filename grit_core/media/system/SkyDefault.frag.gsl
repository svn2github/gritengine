val color = pma_decode(sample2D(mat.emissiveMap, vert.coord0.xy)) * float4(1, 1, 1, mat.alphaMask);
if (color.a <= mat.alphaRejectThreshold) discard;
frag.color = float4(gamma_decode(color.rgb) * mat.emissiveMask, color.a);
