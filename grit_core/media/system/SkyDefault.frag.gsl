val color = pma_decode(sample2D(mat.emissiveMap, in.coord0.xy)) * float4(1, 1, 1, mat.alphaMask);
if (color.a <= mat.alphaRejectThreshold) discard;
out.color = float4(gamma_decode(color.rgb) * mu.emissiveMask, color.a);
