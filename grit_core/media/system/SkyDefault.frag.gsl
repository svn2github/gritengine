val colour = pma_decode(sample2D(mat.emissiveMap, vert.coord0.xy)) * Float4(1, 1, 1, mat.alphaMask);
if (colour.a <= mat.alphaRejectThreshold) discard;
frag.colour = Float4(gamma_decode(colour.rgb) * mat.emissiveMask, colour.a);
