material `concrete` {
    diffuseMask = vec(.5, .5, .5),
}
material `grass` {
    diffuseMask = vec(.35, 1, .1),
}
material `metal` {
    diffuseMask = vec(.25, .25, .25),
}
material `water` {
    diffuseMask = vec(.2, .5, 1),
}

class `citadel_wb` (ColClass) {renderingDistance=600}
class `static_ground_wb` (ColClass) {renderingDistance=600}
class `static_ground_wb2` (ColClass) {renderingDistance=600}
class `static_ground_wb3` (ColClass) {renderingDistance=600}
class `static_ground_angle_wb` (ColClass) {renderingDistance=600}
class `building_block_large` (ColClass) {renderingDistance=600}
class `building_block_med` (ColClass) {renderingDistance=600}
class `building_block_small` (ColClass) {renderingDistance=600}

